# Многопользовательская чат-система (C++ / Docker / SQLite)

Современное клиент-серверное приложение для обмена сообщениями в реальном времени, построенное на архитектуре TCP/IP с использованием Modern C++ (C++17). Проект демонстрирует навыки проектирования распределённых систем, контейнеризации, безопасного сетевого взаимодействия и организации отказоустойчивой серверной логики.

---

## Общий стек технологий

* **Язык:** C++17 (умные указатели, атомарные флаги, RAII)
* **Сборка:** CMake (Modern Presets API: Базовая / Отладка / Релиз)
* **Контейнеризация:** Docker и Docker Compose (многоэтапная сборка, Alpine Linux)
* **Сетевой стек:** POSIX Sockets, DNS-разрешение через `getaddrinfo`
* **Протокол:** JSON over TCP (`nlohmann/json`)
* **Логирование:** `spdlog` (асинхронное потокобезопасное логирование в консоль и файлы)
* **База данных:** SQLite3 (персистентное хранение истории сообщений)

---

## Серверная часть (Backend)

Многопоточный TCP-сервер, выступающий в роли брокера сообщений и подсистемы хранения истории.

### Архитектурные особенности

#### Многопоточность

* Изоляция каждого клиента в отдельном `std::thread`
* Потокобезопасное управление списком клиентских дескрипторов через `std::mutex` и `std::lock_guard`

#### Постоянное хранение истории

* Автоматическое сохранение сообщений в SQLite
* При подключении нового клиента сервер загружает последние 10 сообщений (`ORDER BY id DESC`), обеспечивая сохранение контекста диалога
#### Безопасность
* **Санитизация ввода:** замена ESC-последовательностей (`\033`) на безопасные печатные символы для защиты серверного терминала
* **Защита от SQL-инъекций:** использование prepared statements (`sqlite3_prepare_v2`) и привязки параметров
* **Корректное завершение:** перехват сигнала `SIGINT (Ctrl+C)` с уведомлением клиентов, сохранением состояния и корректным освобождением ресурсов через RAII

---

## Клиентская часть (Frontend CLI)

Интерактивный консольный клиент с поддержкой асинхронного приёма данных.

### Ключевые возможности

#### Неблокирующий интерфейс

* Разделение пользовательского ввода (`std::getline`) и приёма сообщений в независимые потоки

#### Терминальный UX
* Использование ANSI-последовательностей (`\033[2K\r`) для очистки и перерисовки строки ввода
* Входящие сообщения не нарушают вводимый пользователем текст

#### Гибкая конфигурация

* Чтение IP-адреса и порта из `config.json`
* Поддержка подключения как по IP-адресу, так и по доменному имени, что особенно важно при работе в Docker-сетях

#### Безопасное управление ресурсами

* Реализация **правила пяти (Rule of Five)** для безопасного управления сетевым дескриптором

---

## Развёртывание и запуск (Docker)

Проект полностью контейнеризирован. Финальные production-образы на базе Alpine Linux имеют размер менее 20 MB.

### Сборка через Docker Compose

Предпочтительный способ запуска полной инфраструктуры (сервер и клиент):

```bash
# Из корня проекта
docker compose up --build
```
**Примечание:** Чтобы взаимодействовать с клиентом, используйте команду `docker attach chat_client_container` в новом терминале.

### Ручной запуск (режим отладки)

Для тестирования в отдельных терминалах используется выделенная Docker-сеть:

```bash
# 1. Создание сети
docker network create chat_net

# 2. Запуск сервера (Терминал 1)
docker build -t my_server ./server
docker run -it --rm --network chat_net --name chat_server -p 9000:9000 my_server

# 3. Запуск клиента (Терминал 2)
docker build -t my_client ./client
docker run -it --rm --network chat_net --name client_1 my_client
```

**Примечание:** В `client/config.json` адрес сервера должен быть указан как `"chat_server"`.


### Локальная сборка через CMake (без Docker)

Если проект запускается в локальной Linux-среде или любой совместимой POSIX-системе, доступен прямой запуск без контейнеризации:

```bash
cmake --preset default
cmake --build --preset default
```

Данный способ рекомендуется для локальной отладки, профилирования, использования sanitizers и работы в IDE с поддержкой CMake Presets.

---

## Структура проекта

```text
/server               — исходный код сервера, Dockerfile, интеграция с SQLite
/client               — исходный код клиента, Dockerfile, обработка ANSI-последовательностей
/include              — common.hpp с общими структурами данных и протоколом обмена
CMakePresets.json     — стандарты сборки для локальной среды и Docker
```

---

## Ключевые инженерные практики

* Modern C++17
* RAII и управление ресурсами
* Потокобезопасная серверная архитектура
* Безопасная обработка сетевого ввода
* Персистентность через SQLite
* Многоэтапная Docker-сборка
* DNS-aware подключение клиента
* Корректное завершение работы
* Структурированное логирование

---

## Статус репозитория

```text
Система сборки     : CMake Presets
Стандарт языка     : C++17
Платформа          : Linux / Docker
База данных        : SQLite3
Логирование        : spdlog
Сетевое взаимодействие : POSIX TCP Sockets
```

---

## Архитектура системы

```text
+-------------------+        TCP / JSON         +-------------------+
|    Клиент #1      |  <-------------------->   |                   |
+-------------------+                           |                   |
                                                |                   |
+-------------------+        TCP / JSON         | Многопоточный     |
|    Клиент #2      |  <-------------------->   | сервер сообщений  |
+-------------------+                           |                   |
                                                |                   |
+-------------------+        TCP / JSON         |                   |
|    Клиент #N      |  <-------------------->   |                   |
+-------------------+                           +---------+---------+
                                                          |
                                                          v
                                                 +------------------+
                                                 |     SQLite3      |
                                                 | История сообщений|
                                                 +------------------+
```

---

## Сценарий обмена сообщениями

```text
1. Клиент запускается и считывает config.json
2. Устанавливается TCP-соединение
3. На сервер отправляется AUTH-сообщение
4. Сервер восстанавливает последние 10 сообщений из SQLite
5. Начинается двусторонняя чат-сессия в реальном времени
6. При завершении сервер рассылает событие отключения
```

---

## Производительность и надёжность

* Выделенный поток на каждого клиента для изоляции
* Доступ к общему состоянию защищён mutex
* Безопасное владение сокетом через Rule of Five
* Минимальный размер Docker-образа (<20 MB)
* SQLite обеспечивает долговечность сообщений
* Поддержка DNS для контейнерной оркестрации
* Корректная обработка SIGINT

---

## Дорожная карта развития

### Планируемые улучшения

* Переход от модели «поток на клиента» к thread pool
* Масштабируемый ввод-вывод на основе epoll
* TLS-шифрование транспортного уровня
* Токены аутентификации и управление сессиями
* Подтверждения доставки сообщений
* Горизонтальное масштабирование через абстракцию брокера сообщений
* WebSocket-шлюз для браузерных клиентов
* Метрики и интеграция с Prometheus

---

## Стратегия CI / контроля качества

План дальнейшего развития репозитория включает:

* GitHub Actions для автоматической сборки
* Статический анализ (`clang-tidy`, `cppcheck`)
* Проверки через AddressSanitizer / ThreadSanitizer
* Unit- и integration-тесты
* Публикацию Docker-образов
* Стратегию тегирования релизов

---

## Лицензия

Проект предназначен для образовательных целей, портфолио и исследовательских задач. Production-версия репозитория может распространяться по лицензии **MIT**.
